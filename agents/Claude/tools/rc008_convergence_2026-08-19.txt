
1. The two rules are endpoints of one block-interleaved family
PASS  equal halves require p to divide 6,510, which has 32 divisors  [p in 1 ... 6510, 32 members]
PASS  6,510 factors as 2 * 3 * 5 * 7 * 31, so the count is 2**5  [2**5 = 32]
PASS  p = 6,510 IS the midpoint-contiguous rule  [half A is exactly indices 0 ... 6,509]
PASS  p = 1 IS the canonical even/odd rule  [half A is exactly the even indices]
PASS  every member splits the core into two halves of 6,510  [32 of 32 members]
PASS  a member's mask is fixed before any data: same p, same mask  [no member reads the core to be defined]
PASS  so 'the alternative carries a free parameter' names 32 fixed rules, not one  [the free parameter belongs to the family, not to either endpoint]

2. The parameter's decision destination across all 32 members
PASS  Draft 34's parity fixture reproduces at R_space_sampled = 1.5  [R_space = 1.500000000000]
PASS  every member's R_null_sampled is finite on this fixture  [32 of 32 finite, so the counts below are not a NaN artifact]
PASS  R_null_sampled takes exactly two values across the family  [values = [1.0, 4.0]]
PASS  16 members reach `unmeasurable` on byte-identical data  [p = [1, 3, 5, 7, 15, 21, 31, 35, 93, 105, 155, 217, 465, 651, 1085, 3255]]
PASS  16 members reach `passes` on the same data  [p = [2, 6, 10, 14, 30, 42, 62, 70, 186, 210, 310, 434, 930, 1302, 2170, 6510]]
PASS  the family splits on the PARITY OF p on this fixture  [odd p withholds, even p passes]
PASS  the pinned rule p = 6,510 is one of the 16 that pass  [passes]
PASS  the reviewed rule p = 1 is one of the 16 that withhold  [unmeasurable]
PASS  so the choice of member decides the disposition, 16 ways each  [passes vs unmeasurable]

3. What the finding does not reach
PASS  all 32 members are partitions of the identical retained core  [checked by sorting, not asserted]
PASS  so R_space_sampled recomputes to one value from all 32 members  [R_space = 1.500000000000, max deviation 0.000e+00 over 32 members]
PASS  Draft 34's reach recomputes unchanged: 9 moved  [9 moved]
PASS  6 relabelled and 57 untouched  [6 relabelled, 57 untouched]
PASS  and no other transition exists  [0 illegal transitions]
PASS  so a change of member can never cross the failure boundary  [the reach bound survives F8-R3; only its RATIONALE fell]

Summary
22 checks, 0 failed
