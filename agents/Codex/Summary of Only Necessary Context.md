# Summary of Only Necessary Context — Codex

**Rewritten at the end of Codex Session 14 · 2026-08-13 01:10 PDT**

**Next Codex session will be Session 15.** No count-based progress report is due until Session 16.

## Current phase and immovable boundary

**Phase 2 — Execution is open. No scientific result exists.** No host is pinned. No host-specific eligible pool, rendered edge table, donor selection, template-array pull, dependency installation, raw-recording download, Rung 0, hybrid generation or sorter run occurred in Codex Session 14.

The public state remains `In Progress`. Proposed Amendment 6 and the pre-pool matcher are design/governance work, not evidence about whether realism changes measured sorter accuracy.

## Contract state — Amendment 6 is proposed and awaiting Claude

Amendments 1–5 remain `In force`. Amendment 6 remains `Proposed`; it carries no force until Claude genuinely re-reviews and explicitly approves the exact Codex-edited state:

- `Claim Sheet.md`: SHA-256 `8fa0342279cfe4173ebb605fce4d3434e2647533f4af3a444b78a5f286cfaf48`
- `Accessible Claim Sheet.md`: SHA-256 `c58446d64b58748230f019188812a8eb3b043e95f10151ab1420799cab487d68`

Codex accepts Amendment 6's central design:

- `N` is the count of host-eligible injection-zone donors; continue for `10 <= N <= 16`, while `N < 10` triggers Slot 12.3;
- every killed donor and reason is published;
- fifty occurrences use `q = floor(50/N)`, `r = 50 mod N`, with exactly `r` donors appearing `q + 1` times;
- SHA-256 seed `1910753866` orders survivors and a round-robin deal fixes extra occurrences and block membership while keeping ten distinct donors per block;
- real control, pseudo P1 and pseudo P2 all use `N` and the same exposure structure;
- Amendment 5's Z remains the full sixteen zone keys;
- Amendment 5's uniform-draw expectation uses actual arm size `N`, while recorded sixteen-target diagnostics remain historical;
- Slot 13.9 narrows to the exact surviving set plus killed list.

Codex made two exact-state repairs:

1. **No eligibility/rota loop.** Before any donor is evaluated, the Tier A configuration must pin a finite candidate-site set, thresholds, per-site predicates and the site-to-donor reduction. A donor survives iff at least one pinned site passes all per-donor gates. `N` is computed once. After the rota exists, each block's ten donors must admit a jointly feasible placement under the same sites/rule; failure rejects the host and never drops a donor/redeals.
2. **Complete fixed-sixteen supersession.** Amendment 3 point 3's “P1's fixed sixteen” sentence and other explanatory arm/subset/rota/draw descriptions now follow `N`. Historical diagnostics actually computed at sixteen remain history. The library ceiling, pre-host sixteen-donor pool and full Z removal stay sixteen.

Active chat: `chats/Claude-Codex/Tier A Donor Matching Rule/Tier A Donor Matching Rule - Active.md`. Claude's next action is exact owner re-review of the two hashes above. If approved unchanged, Amendment 6 can enter force.

## Real-arm matching rule — Draft 2 remains blocked

Artifact: `agents/Codex/Tier A Real-Arm Donor Matching Rule.md`

Draft 2 SHA-256: `f4ab71c3216d172641706a199d0536520da538aff906eb29d94f06f3d6620310`

Claude explicitly approves Draft 2. Codex does not approve those bytes. Accepted pieces remain common U-derived scaling with an R-derived diagnostic, donor-equal matching, exact source-count equality, exact constrained optimization, concentration outputs, named count comparators, global no-reuse assignment and gate separation.

After Amendment 6 enters force, Draft 3 must:

1. enforce source-count equality at every stage: insertion+count, session+count, subject+count, unrestricted hard-eligible edges+count, then infeasible;
2. generalize every target/candidate cardinality, report and failure test to `N = 10…16`, while keeping Z at all sixteen zone keys;
3. consume the precomputed one-time target eligibility manifest and `N` without creating a second eligibility/redeal loop;
4. replace all literal sixteen assignment/source-count expectations with the surviving target set and its actual distinct-source count;
5. remove the 22% explanation and state only the invariant: with `q = floor(50/N)`, exposure weighting gives the extra-occurrence donors `(q + 1)/q` times the influence of the others;
6. preserve donor-equal matching and exposure-weighted reporting;
7. update outputs and failure semantics for the killed list, `N`, actual source multiplicity and full-Z composition check.

Do not edit Draft 2 into a final state until Amendment 6 converges. The implementation and deterministic tests are a separate same-state review after prose convergence and before any host-specific pool or edge table is constructed or opened.

## Host selection and Rung 0 remain unchanged

`agents/Claude/Tier A Host and Injection Zone Selection.md` Draft 7 at `13c192d3478ffdba35d756715ef2236d52a3cb31e6156dc818fd5c002dd19d01` is same-state approved for its declared strategy/evidence scope, not as a host selection.

Candidate order remains:

1. CSHL047 Probe01, session `b52182e7`;
2. NYU-12 Probe01, session `a8a8af78`;
3. CSHL047 Probe00, session `b52182e7`.

Claude owns drift, noise and post-rescaling effective-SNR host gates. Codex owns the two-part footprint/placement calibration, independent balance/manipulation gate, Rung 0, sorter-panel decision and inference/negative-control harness.

Rung 0 remains unrun. It must construct and pin the pre-injection host substrate, avoid phase-shifting injected templates twice, pin the exact approximately 60-second segment, resolve dependencies in the project venv, and take fresh RAM/VRAM immediately before heavy work under Amendment 1's live resource guards. The current venv still pins only `h5py==3.16.0` and `numpy==2.5.2`.

## Reproducibility Packet state

The scoped design-stage review is concluded. Same-state approved hashes remain:

- packet `README.md`: `3b07aa5b94203d3f4c826be7aac40e6550c2a13e7d3b45c997399a84af3df4a1`
- checker: `4eb9401825ec2a4561abf46dc7dc82d89ea316722d338bba9e84bd747d83c651`
- mutation harness: `d64134b1485a8550412c1eb7fba7fd98ab509471050cad71a41fc8f704d5acc3`

One command per runbook step/example is a hard error. Ten current steps pass; all fifteen mutations are caught; all eleven packet scripts render `--help`. The packet remains incomplete: the five archive-reading steps were not re-run during that review, and the headline pipeline plus Slot 8 `verify_realism.py` do not exist because no result exists. Later packet additions start a new scoped review.

## Gate order — do not collapse

1. Claude owner re-review and exact-state convergence of Amendment 6;
2. Amendment 6 status moved to `In force` in both synchronized sheets;
3. matching Draft 3 prose convergence;
4. matching implementation/test convergence before pool access;
5. host selection and placement calibration;
6. exact U/Z/R manifests, target eligibility manifest, edge table, two matching outputs and selected IDs;
7. independent Tier A balance/manipulation approval;
8. generation authorization;
9. Rung 0/sorter execution authorization.

Reviewer edits, downstream use, later commits and silence do not substitute for same-state approval.

## Public and director state

- Root `README.md` remains State A / `In Progress`. Its latest entry records Claude's proposed Amendment 6. Codex's review did not move it into force, so no new heartbeat was added.
- The Phase 1 director contract-review request remains open and non-blocking.
- No new director action is needed.
- No progress report was due in Session 14.

## Validation and machine record from Session 14

- Both seed derivations reproduced: `1910753866` and `711362139`.
- Rota checks passed for every `N = 10…16`: fifty total occurrences, exact quotient/remainder multiplicities and ten distinct donor ranks in every block.
- Relative to the last in-force contract at `f4419c4`, the sheets remain pure additions: 60/0 technical and 50/0 accessible.
- `git diff --check` passed after the contract edits.
- Append-only chat safeguard passed: the 215-line pre-write transcript is an exact prefix and the Codex Session 14 header occurs exactly once after that boundary.
- 2026-08-13 01:10 PDT: 9.12 GiB RAM free of 31.67; 1,025 MiB VRAM used of 16,311; 604.5 GB free on `C:`. Nothing heavy ran.

`agents/Codex/Session Summaries/HumanReport14.md` contains the full review rationale, exact edits, validation evidence and file list.
